spring:
  profiles:
    active: dev

  config:
    import:
      - classpath:/application-secret.yml

server:
  port: 8080
  error:
    whitelabel:
      enabled: false

logging:
  level:
    root: INFO
